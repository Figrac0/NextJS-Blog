---
title: "Система управления записями - Админ панель"
excerpt: "Готовое к продакшену решение для управления записями с Redux-подобным управлением состоянием, фильтрацией в реальном времени и TypeScript типизацией."
date: "2026-02-01"
slug: "appointment-admin-panel"
image: "appointment-admin-preview.png"
type: "project"
tech:
    [
        "React 18",
        "TypeScript",
        "Custom Hooks",
        "SCSS Modules",
        "Firebase Hosting",
    ]
isFeatured: true
isTrending: true
isNew: false
stats:
    stars: 28
    forks: 5
readingTime: "12 мин"
difficulty: "Средний"
demoUrl: "https://adminpanel-a2867.web.app/schedule"
---

# Система управления записями - Админ панель

**[🚀 Живое демо: Посетите админ панель](https://adminpanel-a2867.web.app/schedule)**

---

## 📸 Предварительный просмотр проекта

![Дашборд с обзором записей](1.png)

---

![Фильтрация по календарю и детали записей](2.png)

---

![Валидация форм и создание записей](3.png)

---

## 🚀 Быстрые ссылки

- **[🌐 Живое демо](https://adminpanel-a2867.web.app/schedule)** - Исследуйте интерфейс администратора
- **[💻 Репозиторий GitHub](https://github.com/Figrac0/Admin-panel)** - Посмотрите исходный код

---

## 📋 Содержание

- 🏗️ Архитектура системы
- 🔄 Дизайн управления состоянием
- 🎯 Основные возможности
- ⚙️ Техническая реализация
- 🧩 Структура компонентов
- 🚀 Оптимизация производительности

---

## 🏗️ Архитектура системы

### **Основа на TypeScript**

Построена полностью на **TypeScript**, обеспечивая безопасность типов во время компиляции и устраняя ошибки времени выполнения. Система использует строгие интерфейсы для данных записей, значений контекста и полезных нагрузок действий.

### **Кастомный слой управления состоянием**

Вместо Redux Toolkit я реализовал **кастомную Redux-подобную архитектуру** с:

- **Context API** в качестве провайдера хранилища
- **useReducer** для предсказуемых обновлений состояния
- **Строгие типы действий** с TypeScript enum
- **Синхронные обновления без middleware**

### **Архитектура потока данных**

```typescript
Запрос API → Кастомный Http хук → Диспатч действия → Редюсер → Обновление состояния → Ре-рендер UI
```

---

## 🔄 Дизайн управления состоянием

### **Реализация контекста записей**

Система использует сложный провайдер контекста, который обрабатывает:

- Двойное состояние записей (все записи vs активные записи)
- Фильтрацию по датам календаря с выбором диапазона
- Отслеживание статуса загрузки (idle, loading, error)
- Конвейеры трансформации данных

```typescript
// Основной интерфейс контекста
interface AppointmentContextValue extends IAppointmentState {
    getAppointments: () => void;
    getActiveAppointments: () => void;
    setDateAndFilter: (newDate: Value) => void;
}

// Редюсер состояния со строгими типами действий
export enum ActionsTypes {
    SET_ACTIVE_APPOINTMENTS = "SET_ACTIVE_APPOINTMENTS",
    SET_ALL_APPOINTMENTS = "SET_ALL_APPOINTMENTS",
    FETCHING_APPOINTMENTS = "FETCHING_APPOINTMENTS",
    ERROR_FETCHING_APPOINTMENTS = "ERROR_FETCHING_APPOINTMENTS",
    SET_CALENDAR_DATE = "SET_CALENDAR_DATE",
}
```

---

### **Система фильтрации данных**

Контекст включает интеллектуальную фильтрацию, которая:

- Фильтрует записи на основе выбранных диапазонов дат календаря
- Отделяет активные записи от отмененных
- Обрабатывает граничные случаи для частичного выбора дат
- Сохраняет целостность данных во время операций фильтрации

---

## 🎯 Основные возможности

### **📅 Интеграция с календарем**

- Интеграция React Calendar с пользовательским выбором диапазона дат
- Фильтрация в реальном времени - записи обновляются немедленно при изменении дат
- Типобезопасная обработка дат с использованием Day.js с кастомным парсингом
- Валидация форматов дат (`ДД/ММ/ГГГГ ЧЧ:мм`) перед отправкой в API

### **⚡ Кастомный слой абстракции HTTP**

Переиспользуемый хук `useHttp`, который предоставляет:

- Управление статусом загрузки (состояния idle, loading, error)
- Централизованную обработку ошибок с согласованной передачей
- Конфигурацию запросов с TypeScript интерфейсами
- Переиспользуемую логику fetch для всех API-вызовов

```typescript
// Интерфейс кастомного хука
interface RequestConfig {
    url: string;
    method?: HTTPRequestMethods;
    body?: string | null;
    headers?: HTTPHeaders;
}

export const useHttp = () => {
    const [loadingStatus, setLoadingStatus] =
        useState<loadingStatusOptions>("idle");

    // Логика запроса с обработкой ошибок
};
```

---

### **🔐 Сервисный слой записей**

Специализированный сервисный слой, который:

- Валидирует ответы API на наличие обязательных полей
- Трансформирует данные между различными форматами
- Обрабатывает бизнес-логику для отмены записей
- Управляет форматированием дат между стандартами UI и API

### **🎯 Управление формами с валидацией**

- Валидация форм в реальном времени с использованием паттернов
- Принудительное форматирование номеров телефонов (+1 890 335 372)
- Валидация форматов дат с кастомным парсингом
- Оптимистичные обновления для лучшего UX

---

## ⚙️ Техническая реализация

### **Полная типобезопасность**

```typescript
// Строгие интерфейсы для данных записей
export interface IAppointment {
    id: number;
    date: string;
    name: string;
    service: string;
    phone: string;
    canceled: boolean;
}

// Интерфейс подмножества активных записей
export interface ActiveAppointment {
    id: number;
    date: string;
    name: string;
    service: string;
    phone: string;
}
```

---

### **Кастомная система порталов**

Реализован переиспользуемый компонент `Portal` для:

- Модальных окон и всплывающих подсказок
- Динамического создания DOM-элементов
- Очистки при размонтировании компонента
- Управления обертками элементов

### **Реализация границ ошибок**

- Индикаторы состояния загрузки во время API-вызовов
- Fallback UI для неудачных запросов
- Механизмы повторных попыток для временных сбоев
- Пользовательские сообщения об ошибках

---

## 🧩 Структура компонентов

### **Паттерн умного контейнера**

Приложение следует паттерну container/presentational:

```text
AppointmentContextProvider (Контейнер)
├── SchedulePage (Контейнер)
│   ├── Calendar (Представление)
│   ├── AppointmentList (Представление)
│   └── Filters (Представление)
└── HistoryPage (Контейнер)
    └── HistoryTable (Представление)
```

## 🧩 Переиспользуемые компоненты

### **CAForm** - Форма создания записей с валидацией

- Валидация форм в реальном времени с использованием паттернов
- Принудительное форматирование номеров телефонов (+1 890 335 372)
- Валидация форматов дат с парсингом Day.js
- Оптимистичные обновления для лучшего UX

### **AppointmentList** - Виртуализированный список записей

- Эффективный рендеринг больших наборов данных записей
- Скелетоны состояний загрузки во время получения данных
- Сортируемые колонки и возможности фильтрации
- Адаптивный дизайн для всех размеров экранов

### **CalendarFilter** - Компонент выбора диапазона дат

- Интеграция React Calendar с кастомной стилизацией
- Выбор диапазона для фильтрации записей
- Валидация дат и обработка ошибок
- Предустановленные диапазоны дат (сегодня, неделя, месяц)

### **LoadingSpinner** - Анимированный индикатор загрузки

- Плавные CSS анимации для лучшего UX
- Настраиваемые размеры и варианты цветов
- Accessibility метки для скринридеров
- Условный рендеринг на основе состояний загрузки

## 🎨 CSS Архитектура

### **SCSS Modules** для изолированных стилей компонентов

- Локальная область видимости CSS для предотвращения конфликтов стилей
- Модульная архитектура для поддерживаемой стилизации
- Поддержка горячей перезагрузки во время разработки
- Компиляция в оптимизированный CSS в продакшене

### **CSS Custom Properties** для темизации

- Динамические переменные темы (готово к светлой/темной теме)
- Согласованная цветовая палитра между компонентами
- Возможность переключения тем во время выполнения
- Fallback значения для совместимости с браузерами

### **BEM методология** для именования классов

- Конвенция именования Block\_\_Element--Modifier
- Предсказуемая и масштабируемая CSS архитектура
- Простота отладки и поддержки
- Стандартизация для командной работы

### **Адаптивные брейкпоинты** для мобильной совместимости

- Mobile-first адаптивный дизайн
- Брейкпоинты: мобильный (<768px), планшет (768-1024px), десктоп (>1024px)
- Элементы интерфейса, удобные для касаний
- Адаптивные макеты для разных размеров экранов

## 🚀 Оптимизация производительности

### **Эффективные обновления состояния**

- **Селективные ре-рендеры** с использованием разделения значений контекста
- **Мемоизированные callback функции** для предотвращения ненужных ре-рендеров
- **Ленивая загрузка** для некритических компонентов
- **Нормализация данных** для избежания дублирования состояния

### **Оптимизация API**

- **Дедупликация запросов** для идентичных API-вызовов
- **Умное кэширование** данных записей
- **Пакетные обновления** там, где возможно
- **Прогрессивная загрузка** списков записей

### **Управление размером бандла**

- **Tree shaking** для неиспользуемых импортов
- **Разделение кода** по роутам
- **Ленивая загрузка** тяжелых зависимостей
- **Минифицированные продакшен сборки**

## 🛠️ Опыт разработки

### **Конфигурация TypeScript**

- **Включен strict mode** для максимальной типобезопасности
- **Псевдонимы путей** для чистых импортов
- **ESLint + Prettier** для согласованности кода
- **Кастомные определения типов** для сторонних библиотек

### **Интеграция Mock API**

- **MockAPI.io** для разработки и тестирования
- **Согласованные схемы ответов**
- **Симуляция сценариев ошибок**
- **Тестирование ограничения скорости**

## 🔧 Стратегия деплоя

### **Firebase Hosting**

- **Автоматические SSL сертификаты**
- **Глобальное распределение CDN**
- **Конфигурация кастомного домена**
- **Непрерывный деплой** из GitHub

### **Конфигурация окружения**

- **Раздельные окружения** для разработки и продакшена
- **Конфигурация API эндпоинтов** через переменные окружения
- **Система feature flags** для постепенных релизов

## 📊 Технические метрики

- **Покрытие TypeScript**: 100% основной логики
- **Размер бандла**: ~45KB gzipped
- **First Contentful Paint**: < 1.5s
- **Время ответа API**: < 200ms
- **Покрытие тестами**: Unit тесты для управления состоянием

## 🎯 Ключевые технические решения

1. **Кастомное управление состоянием** вместо Redux Toolkit для лучшего контроля и меньшего размера бандла
2. **TypeScript strict mode** для предотвращения ошибок времени выполнения и улучшения опыта разработки
3. **Кастомный HTTP хук** вместо axios для HTTP запросов без зависимостей
4. **SCSS Modules** вместо styled-components для лучшей производительности и контроля CSS
5. **Day.js** вместо Moment.js для меньшего размера бандла и современного API

## 🔮 Планы по улучшению

Запланированные улучшения включают:

- **Интеграцию WebSocket** для обновлений записей в реальном времени
- **Контроль доступа на основе ролей** для многопользовательских административных сред
- **Продвинутую аналитическую панель** для метрик записей
- **Функциональность экспорта** для CSV/PDF отчетов
- **Мобильное приложение** с использованием React Native

---

**🚀 Готовы исследовать код?**

**[Живое демо](https://adminpanel-a2867.web.app/schedule)** • **[Репозиторий GitHub](https://github.com/Figrac0/Admin-panel)**
